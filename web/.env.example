# BlockWiki frontend — Vercel environment (TEMPLATE)
# Set these in the Vercel project dashboard (Settings > Environment Variables),
# NOT in this file. Values shown are placeholders.

# Public URL of the deployed Strapi backend (Oracle VM + nginx + SSL)
STRAPI_URL=https://api.yourdomain.com

# Secret used by the on-demand revalidation webhook (if used)
REVALIDATE_SECRET=change-me-please
